[project]
name = "local-shazam"
dynamic = ["version"]
description = "Image transformation server"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28",
    "pydantic>=2.10",
    "pydantic-settings>=2.6",
    "structlog>=24.4",
    "anyio>=4.0",
    "openai>=1.0",
    "pillow>=10.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "python-multipart>=0.0.18",
]

[dependency-groups]
dev = [
    "hypothesis>=6.120",
    "import-linter>=2.15",
    "mutmut>=3.8.0",
    "mypy>=1.13",
    "pip-audit>=2.7",
    "pre-commit>=4.0",
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=6.0",
    "pytest-xdist>=3.5",
    "respx>=0.22",
    "ruff>=0.8",
    "vulture>=2.16",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
local-shazam-server = "local_shazam.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/local_shazam"]

[tool.hatch.version]
path = "src/local_shazam/__init__.py"

[tool.ruff]
target-version = "py313"
src = ["src"]
extend-exclude = ["vulture_whitelist.py"]

[tool.ruff.lint]
select = [
    "E", "F", "I", "UP", "B", "SIM", "RUF", "PTH", "PERF", "S", "ASYNC",
    "A", "C4", "DTZ", "PIE", "RET", "TCH", "ARG", "FURB",
]
ignore = ["E501", "S101"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101", "S105", "S106", "ARG001"]

[tool.ruff.lint.isort]
known-first-party = ["local_shazam"]

[tool.mypy]
mypy_path = "src"
strict = true
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_mode = "auto"
addopts = "-ra -q --strict-markers -m 'not e2e'"
markers = ["slow", "integration", "e2e: starts the real server process and calls it over HTTP"]
filterwarnings = ["error"]
xfail_strict = true

[tool.coverage.run]
source = ["src"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]

[tool.vulture]
paths = ["src", "vulture_whitelist.py"]

[tool.mutmut]
source_paths = ["src/"]
